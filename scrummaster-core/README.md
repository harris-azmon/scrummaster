# Scrummaster Core

Platform-agnostic core logic for Scrummaster. This package contains the data models, prompt rendering, and VCS abstraction layers (Fossil, Git, Jujutsu) used by all Scrummaster adapters. Fossil is the default backend; see `fossil_service.py`.
