description = "Context-driven development methodology. Understands projects set up with Scrummaster (via Gemini CLI or Claude Code). Use when working with scrummaster/ directories, stories, specs, plans, or when user mentions context-driven development."

prompt = """
﻿---
name: scrummaster
description: Context-driven development methodology. Understands projects set up with Scrummaster (via Gemini CLI or Claude Code). Use when working with scrummaster/ directories, stories, specs, plans, or when user mentions context-driven development.
license: Apache-2.0
compatibility: Works with Claude Code, Gemini CLI, and any Agent Skills compatible CLI
metadata:
  version: "0.1.0"
  author: "Gemini CLI Extensions"
  repository: "https://github.com/gemini-cli-extensions/scrummaster"
  keywords:
    - context-driven-development
    - specs
    - plans
    - stories
    - tdd
    - workflow
---

# Scrummaster: Context-Driven Development

Measure twice, code once.

## Overview

Scrummaster enables context-driven development by:
1. Establishing project context (product vision, tech stack, workflow)
2. Organizing work into "stories" (features, bugs, improvements)
3. Creating specs and phased implementation plans
4. Executing with TDD practices and progress tracking

**Interoperability:** This skill understands scrummaster projects created by either:
- Gemini CLI extension (`/scrummaster:setup`, `/scrummaster:newTrack`, etc.)
- Claude Code commands (`/scrummaster-setup`, `/scrummaster-newtrack`, etc.)

Both tools use the same `scrummaster/` directory structure.

## When to Use This Skill

Automatically engage when:
- Project has a `scrummaster/` directory
- User mentions specs, plans, stories, or context-driven development
- User asks about project status or implementation progress
- Files like `scrummaster/stories.md`, `scrummaster/product.md` exist
- User wants to organize development work

## Slash Commands

Users can invoke these commands directly:

| Command | Description |
|---------|-------------|
| `/scrummaster-setup` | Initialize project with product.md, tech-stack.md, workflow.md |
| `/scrummaster-newtrack [desc]` | Create new feature/bug story with spec and plan |
| `/scrummaster-implement [id]` | Execute tasks from story's plan |
| `/scrummaster-status` | Display progress overview |
| `/scrummaster-revert` | Git-aware revert of work |

## Scrummaster Directory Structure

When you see this structure, the project uses Scrummaster:

```
scrummaster/
â”œâ”€â”€ product.md              # Product vision, users, goals
â”œâ”€â”€ product-guidelines.md   # Brand/style guidelines (optional)
â”œâ”€â”€ tech-stack.md           # Technology choices
â”œâ”€â”€ workflow.md             # Development standards (TDD, commits, coverage)
â”œâ”€â”€ stories.md               # Master story list with status markers
â”œâ”€â”€ setup_state.json        # Setup progress tracking
â”œâ”€â”€ code_styleguides/       # Language-specific style guides
â””â”€â”€ stories/
    â””â”€â”€ <story_id>/         # Format: shortname_YYYYMMDD
        â”œâ”€â”€ metadata.json   # Story type, status, dates
        â”œâ”€â”€ spec.md         # Requirements and acceptance criteria
        â””â”€â”€ plan.md         # Phased task list with status
```

## Status Markers

Throughout scrummaster files:
- `[ ]` - Pending/New
- `[~]` - In Progress
- `[x]` - Completed (often followed by 7-char commit SHA)

## Reading Scrummaster Context

When working in a Scrummaster project:

1. **Read `scrummaster/product.md`** - Understand what we're building and for whom
2. **Read `scrummaster/tech-stack.md`** - Know the technologies and constraints
3. **Read `scrummaster/workflow.md`** - Follow the development methodology (usually TDD)
4. **Read `scrummaster/stories.md`** - See all work items and their status
5. **For active work:** Read the current story's `spec.md` and `plan.md`

## Workflow Integration

When implementing tasks, follow `scrummaster/workflow.md` which typically specifies:

1. **TDD Cycle:** Write failing test â†’ Implement â†’ Pass â†’ Refactor
2. **Coverage Target:** Usually >80%
3. **Commit Strategy:** Conventional commits (`feat:`, `fix:`, `test:`, etc.)
4. **Task Updates:** Mark `[~]` when starting, `[x]` when done + commit SHA
5. **Phase Verification:** Manual user confirmation at phase end

## Gemini CLI Compatibility

Projects set up with Gemini CLI's Scrummaster extension use identical structure.
The only differences are command syntax:

| Gemini CLI | Claude Code |
|------------|-------------|
| `/scrummaster:setup` | `/scrummaster-setup` |
| `/scrummaster:newTrack` | `/scrummaster-newtrack` |
| `/scrummaster:implement` | `/scrummaster-implement` |
| `/scrummaster:status` | `/scrummaster-status` |
| `/scrummaster:revert` | `/scrummaster-revert` |

Files, workflows, and state management are fully compatible.

## Example: Recognizing Scrummaster Projects

When you see `scrummaster/stories.md` with content like:

```markdown
## [~] Story: Add user authentication
*Link: [scrummaster/stories/auth_20241215/](scrummaster/stories/auth_20241215/)*
```

You know:
- This is a Scrummaster project
- There's an in-progress story for authentication
- Spec and plan are in `scrummaster/stories/auth_20241215/`
- Follow the workflow in `scrummaster/workflow.md`

## References

For detailed workflow documentation, see [references/workflows.md](references/workflows.md).
"""
