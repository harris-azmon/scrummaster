# Network name - if you want to communicate to the server running in a separate devcontainer
# Find this with docker network list, then source .env and restart the devcontainer / devpod
DEVCONTAINER_NETWORK=default-se-7d2ef_default
# http://caddy:80/api/v1
ACAI_API_BASE_URL=https://app.acai.sh/api/v1
ACAI_API_TOKEN=at_123-secret-do-not-publish-me
