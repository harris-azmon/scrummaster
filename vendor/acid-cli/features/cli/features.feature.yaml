feature:
    name: features
    product: cli
    version: 1.0.0
    description: |
        The `acai features` command returns a lightweight worklist of features for one implementation.
        It helps AI agents discover what exists and prioritize what to work on next.

components:
    MAIN:
        description: Primary command behavior
        requirements:
            1: Exposes `acai features`
            2: Accepts optional `--product <product-name>`
            2-1: When `--product` is omitted and no namespaced `--impl` supplies it, the command resolves a target product from the current repo and branch before listing features
            2-2: When `--product` is omitted and no namespaced `--impl` supplies it, and branch-based discovery does not narrow to exactly one product, the command exits non-zero with a targeting error
            3: Accepts optional `--impl <implementation-name>` or namespaced selector `<product/implementation>`
            4: Accepts optional `--status <value>` filters, repeatable
            5: Accepts optional `--changed-since-commit <commit>`
            6: Supports `--json` output for machine consumption
            7: When `--status` is repeated, the command sends all provided status filters to the API
            8: When `--changed-since-commit` is provided, the command sends that filter to the API

    API:
        description: Interaction with the implementation features API
        requirements:
            1: After target resolution, the command calls `GET /api/v1/implementation-features`
            2: The command relays feature completion counts, ref counts, inheritance indicators, and canonical spec commit metadata from the API response

constraints:
    UX:
        requirements:
            1: The default text output presents each returned feature with its name, completion counts, and ref counts
            2: The command is read-only and never mutates specs, refs, or states
            3: The default text output preserves the feature order returned by the API response
            4: When no features match, the command still exits successfully and indicates that no features were returned
            5: When `--json` is active, the final JSON payload includes the full API response data for the selected implementation
